services:
  - type: web
    name: ai-trading-sandbox
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 300 render_app:app
    healthCheckPath: /healthz
