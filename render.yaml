services:
  - type: web
    name: ai-trading-sandbox
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir /opt/render/project/src --bind 0.0.0.0:$PORT --workers 1 --timeout 300 ai_trading_society.web.render_app:app
    healthCheckPath: /healthz
