# Core simulation: no external dependencies.

# Web dashboard (python run.py) - the default entry point.
flask>=3.0

# Optional extras are declared in pyproject.toml:
#   pip install -e ".[viz]"   # matplotlib charts
#   pip install -e ".[config]"  # YAML scenario configs
#   pip install -e ".[ai]"    # OpenAI / Anthropic / Gemini SDKs
#   pip install -e ".[dev]"   # pytest, ruff, mypy
#   pip install -e ".[all]"   # everything