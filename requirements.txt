# Core: no external dependencies required for basic simulation.

# Optional: visualization (charts and graphs)
matplotlib>=3.7

# Optional: scenario config files (YAML)
pyyaml>=6.0

# Optional: external AI agents
openai>=1.0
anthropic>=0.18
google-generativeai>=0.3
python-dotenv>=1.0

# Optional: web UI dashboard
flask>=3.0

# Optional: development
pytest>=7.0
pytest-cov>=4.0
ruff>=0.1
mypy>=1.0
